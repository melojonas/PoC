import './index.css';

/**
 * Componente Header
 * 
 * @component
 * @returns {JSX.Element} - Elemento JSX do cabeçalho
 */
const Header = () => {
    return (
        <header>
            <h1>Prova de Conceito - Frontend</h1>
        </header>
    );
}

export default Header;
